import React from 'react';

function RecipeCards(recipes) {
    // const { first, second, third } = recipes.recipes;
    
    console.log('In RecipeCards', recipes.recipes.data.hits);

    return (
        <div>
            {/* <p>{data}</p> */}
        </div>
    )
}

export default RecipeCards